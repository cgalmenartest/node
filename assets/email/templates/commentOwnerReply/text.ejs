Hello<% if (metadata.recipient.name) { %> <%= metadata.recipient.name %><% } %>,

<% if (metadata.commentUser.name) { %><%= metadata.commentUser.name %><% } else { %>Someone<% } %> <% if (metadata.parentComment) { %>commented in your <% } else { %>started a discussion in your <% } %><% if (!_.isUndefined(metadata['project'])) { %>project<% } else { %>opportunity<% } %>. They said:

> <%- callerComment.trim()  %>

View the discussion at <%= link %>#comment-<% metadata.comment.id %>

Thanks!
- The <%= globals.systemName %> Team