Hello<% if (metadata.recipient.name) { %> <%= metadata.recipient.name %><% } %>,

<% if (metadata.commentUser.name) { %><%= metadata.commentUser.name %><% } else { %>Someone<% } %> <% if (metadata.parentComment) { %>commented in your project.<% } else { %>started a discussion in your project.<% } %> They said:

> <%- callerComment.trim()  %>

View the discussion at <%= projectLink %>

Thanks!
- The <%= globals.systemName %> Team