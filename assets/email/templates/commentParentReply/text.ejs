Hello<% if (metadata.recipient.name) { %> <%= metadata.recipient.name %><% } %>,

<% if (metadata.commentUser.name) { %><%= metadata.commentUser.name %><% } else { %>Someone<% } %> replied to your comment.  They said:

> <%- callerComment.trim()  %>

In reply to your comment:

> <%- parentComment.trim() %>

View the discussion at <%= link %>#comment-<%= metadata.comment.id %>

Thanks!
- The <%= globals.systemName %> Team